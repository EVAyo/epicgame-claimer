name: Epic Games Claim Action

on:
  push
  
jobs:
  setup-environment:
    runs-on: ubuntu-latest
    steps:
    - name: Install Ubuntu packages
      run: |
        sudo apt update && sudo apt install -y python3 python3-pip libnss3 xvfb gconf-service libasound2 \

